spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.hbm2ddl.auto=update

spring.datasource.url=jdbc:mysql://143.106.241.4:3306/dona2006

spring.datasource.username=cl202195

spring.datasource.password=cl*19122006

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

server.address=0.0.0.0
server.port=8080

# Em produção, substitua por uma variável de ambiente com pelo menos 32 caracteres.
app.jwt.secret=${JWT_SECRET:estufa-smart-chave-local-2026-com-mais-de-32-caracteres}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:43200000}
app.jwt.enforce=${JWT_ENFORCE:false}

# Opcional: habilita relatório botânico gerado por IA compatível com OpenAI.
app.ai.api-key=${AI_API_KEY:}
app.ai.provider=${AI_PROVIDER:gemini}
app.ai.endpoint=${AI_ENDPOINT:}
app.ai.model=${AI_MODEL:gemini-2.5-flash}